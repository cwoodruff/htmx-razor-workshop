@page
@model IndexModel
@{
    ViewData["Title"] = "Home • htmx Razor Pages Workshop";
}

<div class="workshop-hero p-4 p-md-5 rounded-4 shadow-sm">
    <div class="row align-items-center g-4">
        <div class="col-lg-7">
            <h1 class="display-6 fw-semibold mb-3">
                Build modern UI without the SPA tax.
            </h1>
            <p class="lead mb-4">
                This workshop teaches a server-driven, hypermedia-first approach using
                <strong>ASP.NET Core Razor Pages</strong> and <strong>htmx</strong>.
                You'll build interactivity by swapping <em>HTML fragments</em>, not constructing client-side state machines.
            </p>
            <div class="d-flex flex-wrap gap-2">
                <a class="btn btn-primary btn-lg" asp-page="/Labs">Start the labs</a>
                <a class="btn btn-outline-light btn-lg" asp-page="/Tasks/Index">Open Tasks demo</a>
            </div>
        </div>
        <div class="col-lg-5">
            <div class="workshop-codecard rounded-4 p-3 p-md-4">
<pre class="mb-0"><code>&lt;input hx-post="?handler=ValidateTitle"
       hx-trigger="keyup changed delay:500ms"
       hx-target="#title-validation"
       hx-include="closest form" /&gt;</code></pre>
                <div class="small text-muted mt-3">
                    Real-time validation without client-side JavaScript.
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-3 g-md-4 mt-1">
    <div class="col-md-4">
        <div class="card workshop-card h-100">
            <div class="card-body">
                <div class="workshop-kicker">Mindset</div>
                <h2 class="h5">Hypermedia-first</h2>
                <p class="text-muted mb-0">
                    Treat HTML as the application contract. Links and forms drive state transitions.
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card workshop-card h-100">
            <div class="card-body">
                <div class="workshop-kicker">Technique</div>
                <h2 class="h5">Fragments and swaps</h2>
                <p class="text-muted mb-0">
                    Return partial HTML from handlers and swap it into stable targets.
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card workshop-card h-100">
            <div class="card-body">
                <div class="workshop-kicker">Outcome</div>
                <h2 class="h5">Less JavaScript</h2>
                <p class="text-muted mb-0">
                    Keep logic on the server, minimize client complexity, and ship faster.
                </p>
            </div>
        </div>
    </div>
</div>
