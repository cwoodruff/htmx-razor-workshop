@page "{handler?}/{Id:int?}"
@model htmx_examples.Pages.EditRow.IndexModel

@{
    ViewData["Title"] = "Index";
}

<!-- Main Content -->
<div id="content">
    <!-- Begin Page Content -->
    <div class="container-fluid">
        <!-- Page Heading -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h4 class="m-0 font-weight-bold text-primary">Edit Row</h4>
            </div>
            <div class="card-body">

                @if (Model?.Contacts?.Count() > 0)
                {
                    <div class="table-responsive">
                        <table class="table table-bordered delete-row-example">
                            <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th></th>
                            </tr>
                            </thead>
                            <tbody hx-target="closest tr" hx-swap="outerHTML" hx-headers='{"RequestVerificationToken": "@Model.RequestToken"}'>
                            @foreach (var contact in Model.Contacts)
                            {
                                <partial name="_TableRow" model="@contact"/>
                            }
                            </tbody>
                        </table>
                    </div>
                }

            </div>
        </div>

    </div>
    <!-- /.container-fluid -->
</div>
<!-- End of Main Content -->